# 📁 مسیرهای ذخیره‌سازی MBNDL

این سند قرارداد فعلی ذخیره‌سازی نسخهٔ `1.0.0` را توضیح می‌دهد.

## Android 10 و جدیدتر

- شناسهٔ برنامه `com.mbn.dl` است.
- دادهٔ موقت و قابل Resume در فضای اختصاصی برنامه و پوشهٔ `MBNDL` نگه‌داری می‌شود.
- فایل‌های تکمیل‌شده با MediaStore در پوشهٔ عمومی `Downloads/MBNDL` منتشر می‌شوند.
- خروجی‌ها برحسب نوع در `Video`، `Audio`، `Subtitle` و `Cover` قرار می‌گیرند.
- برنامه مجوز `MANAGE_EXTERNAL_STORAGE` یا `READ_MEDIA_*` درخواست نمی‌کند.
- روی Android 13 و جدیدتر، مجوز اعلان برای نمایش پیشرفت دانلود در صفحهٔ شروع درخواست می‌شود.

## Android 9 و قدیمی‌تر

برای انتشار خروجی در Downloads، مجوز قدیمی Storage فقط تا API 28 تعریف و در صفحهٔ شروع درخواست می‌شود.

## Windows

- مسیر پیش‌فرض خروجی `Downloads/MBNDL` است.
- مسیر قابل تغییر است و پوشه‌های `Video`، `Audio`، `Cover` و `temp` درون آن ساخته می‌شوند.
- تنظیمات، لاگ، دیتابیس، yt-dlp و FFmpeg قابل‌به‌روزرسانی برای سازگاری با نصب‌های قبلی در `%APPDATA%/MBNDownloader` نگه‌داری می‌شوند.
- نسخهٔ Release در اولین اجرا بدون اینترنت از yt-dlp و آرشیو مینیمال شامل فقط `ffmpeg.exe` و `ffprobe.exe` استفاده می‌کند.
- آپدیتر FFmpeg با HTTP Range فقط دو فایل لازم را از ZIP رسمی دریافت می‌کند.

## History

مسیر فایل اصلی، کاور، زیرنویس‌ها، فایل‌های مرتبط و URI نسخه‌های منتشرشدهٔ Android در SQLite ذخیره می‌شود. حذف فایل از History می‌تواند همهٔ این موارد را هماهنگ پاک کند.
